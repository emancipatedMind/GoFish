<Window x:Class="GoFish.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GoFish"
        mc:Ignorable="d"
        Title="Go Fish!" Height="600" Width="525" Background="Gray" >
        <Grid Margin="10" DataContext="{StaticResource ResourceKey=game }">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*" /> 
                <ColumnDefinition Width="40" /> 
                <ColumnDefinition Width="2*" /> 
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> 
                <RowDefinition Height="Auto" /> 
                <RowDefinition Height="Auto" /> 
                <RowDefinition /> 
                <RowDefinition Height="Auto" /> 
                <RowDefinition Height="Auto" MinHeight="150" /> 
                <RowDefinition Height="Auto" /> 
            </Grid.RowDefinitions>

        <TextBlock Text="Your Name" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <Style TargetType="Button">
                    <Setter  Property="HorizontalAlignment" Value="Center"/>
                    <Setter  Property="MinWidth" Value="90"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding GameInProgress}" Value="True">
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Resources>
            <TextBox x:Name="textName" FontSize="12" Width="150" Text="" IsEnabled="True" />
            <Button Grid.Column="1" Content="Change Name" Command="{Binding ChangeName}" CommandParameter="{Binding Path=Text, ElementName=textName}" />
            <Button Grid.Column="2" Content="Start the game!" Command="{Binding GameStart}" />
        </Grid>

        <TextBlock Text="Game progress" Grid.Row="2" Margin="0,10,0,0" />
        <ScrollViewer x:Name="textProgress" Grid.Row="3" FontSize="12" Background="White" Foreground="Black" Content=""/>

        <TextBlock Text="Books" Grid.Row="4" Margin="0,10,0,0" />
        <ScrollViewer x:Name="textBooks" FontSize="12" Background="White" Foreground="Black" Grid.Row="5" Grid.RowSpan="2" Content=""/>

        <TextBlock Text="Your Hand" Grid.Row="0" Grid.Column="2" />
        <ListBox x:Name="listHand" Background="White" FontSize="12" Height="Auto" Margin="0,0,0,10" Grid.Row="1" Grid.RowSpan="5" Grid.Column="2" IsEnabled="True" />

        <Button x:Name="buttonAsk" Content="Ask for a card" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="6" Grid.Column="2" IsEnabled="True" />
    </Grid>
</Window>